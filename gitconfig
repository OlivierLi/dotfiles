# Add this in your global gitconfig :)
#  [include]
#    path = ~/git/dotfiles/gitconfig

[diff]
	tool = vimdiff
[difftool]
	prompt = false
[alias]
	d = difftool
    squash = "!git rebase -i HEAD~$(git rev-list --count master...)"
